{
  "name": "markquote",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "vitest",
    "build:content": "tsc src/content.ts --outDir dist",
    "build:background": "tsc src/background.ts --outDir dist",
    "build:selection": "tsc src/selection.ts --outDir dist",
    "build:offscreen": "tsc src/offscreen.ts --outDir dist",
    "build:settings": "tsc src/settings.ts --outDir dist",
    "build": "pnpm build:content && pnpm build:background && pnpm build:selection && pnpm build:offscreen && pnpm build:settings"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/turndown": "^5.0.5",
    "jsdom": "^26.1.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "turndown": "^7.2.1"
  }
}